test_and_build:
  stage: build
  tags: 
    - Docker
  script:
    - docker build -t abudimo/snapsearch:latest /snap_search/backend/image_embeddings
    - docker push abudimo/snapsearch:latest

    


