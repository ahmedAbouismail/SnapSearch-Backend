<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="Snapsearch.ResultsPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pancakeView="clr-namespace:Xamarin.Forms.PancakeView;assembly=Xamarin.Forms.PancakeView"
    xmlns:viewmodels="clr-namespace:Snapsearch.ViewModels"
    NavigationPage.HasNavigationBar="false">

    <!--  todo - fix layout properties  -->

    <!--  Implement MainViewModel in the page  -->
    <ContentPage.BindingContext>
        <viewmodels:MainViewModel />
    </ContentPage.BindingContext>

    <ContentPage.Content>
        <AbsoluteLayout>
            <!--  Elements that are not results  -->
            <StackLayout AbsoluteLayout.LayoutBounds="100,0">
                <!--  Logo image  -->
                <ImageButton BackgroundColor="Transparent" Source="snapsearch_logo" />
                <!--  The image that was searched  -->
                <Image Source="snapsearch_image_generic" />
                <!--  Text for informing the user with the amount of search results  -->
                <Label Text="We found 10 images" />
            </StackLayout>

            <ScrollView AbsoluteLayout.LayoutBounds="20, 220, 380, 400" BackgroundColor="White">
                <FlexLayout
                    BindableLayout.ItemsSource="{Binding Images}"
                    Direction="Row"
                    HeightRequest="1000"
                    JustifyContent="SpaceAround"
                    WidthRequest="500"
                    Wrap="Wrap">
                    <BindableLayout.ItemTemplate>
                        <DataTemplate>
                            <pancakeView:PancakeView
                                Margin="10"
                                BackgroundColor="CadetBlue"
                                CornerRadius="10"
                                HeightRequest="160"
                                VerticalOptions="Center"
                                WidthRequest="160">
                                <Grid>
                                    <BoxView
                                        HeightRequest="30"
                                        HorizontalOptions="End"
                                        Opacity=".5"
                                        VerticalOptions="Start"
                                        WidthRequest="30"
                                        Color="White" />
                                    <StackLayout>
                                        <Label
                                            Margin="20,120"
                                            FontSize="10"
                                            Text="{Binding ImageName}" />
                                    </StackLayout>
                                </Grid>
                            </pancakeView:PancakeView>
                        </DataTemplate>
                    </BindableLayout.ItemTemplate>
                </FlexLayout>
            </ScrollView>
        </AbsoluteLayout>
    </ContentPage.Content>
</ContentPage>